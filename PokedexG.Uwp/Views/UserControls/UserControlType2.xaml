<UserControl
    x:Class="PokedexG.Uwp.Views.UserControls.UserControlType2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="100"
    Height="24"
    d:DataContext="{Binding}" RenderTransformOrigin="0.5,0.5">

    <Border Width="{Binding Width}"
            Height="{Binding Height}"
            Background="{Binding Background, FallbackValue=#FF0FFF0F}"
            CornerRadius="12">
        <TextBlock Text="{Binding Text, FallbackValue=MyType}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"
                   FontFamily="Segoe UI Semibold" FontSize="{Binding FontSize, FallbackValue=14}" />
    </Border>
</UserControl>