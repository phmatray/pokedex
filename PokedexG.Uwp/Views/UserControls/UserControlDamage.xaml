<UserControl
    x:Class="PokedexG.Uwp.Views.UserControls.UserControlDamage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="100"
    Height="24"
    d:DataContext="{Binding}">

    <Border Width="{Binding Width}"
            Height="{Binding Height}"
            Background="{Binding Background, FallbackValue=#FF0F0FFF}"
            CornerRadius="12">
        <TextBlock Text="{Binding Text, FallbackValue=Damage}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"
                   FontFamily="Segoe UI Semibold" FontSize="14" />
    </Border>
</UserControl>