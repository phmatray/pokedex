<Page x:Class="PokedexG.Uwp.Views.TypesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModels="using:PokedexG.Uwp.ViewModels"
      xmlns:userControls="using:PokedexG.Uwp.Views.UserControls"
      mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:TypesPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveVisualStateGroup">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <!--<VisualState.Setters>
                          TODO: change properties for narrow view  
                    </VisualState.Setters>-->
                </VisualState>
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <!--<VisualState.Setters>
                          TODO: change properties for normal view  
                    </VisualState.Setters>-->
                </VisualState>
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <!--<VisualState.Setters>
                          TODO: change properties for wide view  
                    </VisualState.Setters>-->
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!--  header  -->
        <controls:PageHeader x:Name="pageHeader" Frame="{x:Bind Frame}"
                             RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True"
                             Text="Forces et faiblesses des types"/>

        <Grid RelativePanel.AlignLeftWithPanel="True"
              RelativePanel.AlignRightWithPanel="True"
              RelativePanel.AlignBottomWithPanel="True"
              RelativePanel.Below="pageHeader">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <GridView Grid.Row="0" ItemsSource="{x:Bind ViewModel.Types}" Margin="100,0,0,0" Padding="0" Height="50" SelectionMode="None">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:TypeLite">
                        <userControls:UserControlType TypeId="{x:Bind Id, FallbackValue=1}" 
                                                      Text="{x:Bind Name, FallbackValue='MyType'}"
                                                      Padding="0,0,-3,0" Width="50" FontSize="10"
                                                      Tapped="Type2_OnTapped"/>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.TypeRelationsGroups}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:TypeRelationGroup">
                        <StackPanel Orientation="Horizontal" Tapped="Type_OnTapped">
                            <userControls:UserControlType TypeId="{x:Bind Type.Id, FallbackValue=1}"
                                                      Text="{x:Bind Type.Name, FallbackValue='MyType'}"
                                                      Margin="0,-10,8,-10" Width="80"/>
                            <GridView ItemsSource="{x:Bind TypeRelations}" Margin="0" Padding="0" VerticalContentAlignment="Center"
                                      SelectionMode="None">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="models:TypeRelation">
                                        <userControls:UserControlDamageFactor DamageFactor="{x:Bind DamageFactor, FallbackValue=0}"
                                                                              Width="38" Height="38" Margin="6,0">
                                            <ToolTipService.ToolTip>
                                                <TextBlock Text="{x:Bind Sentence}" TextWrapping="Wrap"/>
                                            </ToolTipService.ToolTip>
                                        </userControls:UserControlDamageFactor>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </RelativePanel>
</Page>







